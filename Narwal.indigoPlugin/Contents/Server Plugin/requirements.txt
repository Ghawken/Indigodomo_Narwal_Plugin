# Third-party libraries required by the vendored narwal_client.
# Indigo 2024.2+ auto-installs these into the plugin's Packages folder
# and adds it to sys.path on plugin load (no manual installer needed).
#
# Pinned to the same ranges the upstream Home Assistant integration uses
# (see NarwalIntegration/requirements.txt) so vendored client + deps match.
websockets>=12.0,<14.0
protobuf>=4.25.0,<6.0
bbpb>=1.4.0
Pillow>=9.0.0
